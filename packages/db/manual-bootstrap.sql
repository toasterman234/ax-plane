-- Optional manual bootstrap for AxPlane MVP.
-- Preferred path: pnpm db:generate && pnpm db:migrate.
-- This file is here for quick inspection or emergency manual setup.
CREATE EXTENSION IF NOT EXISTS pgcrypto;
